//"n" to execute and move to next line, n -> next, c -> continue
//"list(n)" to list "n" lines
//"repl" to enter read-eval-print-loop
var person = {
    name: "Sankar"
};

person.age = 22;

debugger;//To break on line when in debug mode, "c"

person.name = "Sanku";

console.log(person);